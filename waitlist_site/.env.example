# Redirect links: API the /g/:slug page uses to resolve slugs.
# Use the same base URL you use with curl (e.g. dev node-service).
# Unset: local dev uses http://localhost:3000/v1; production uses https://dev-api.altyr.com/v1
# VITE_REDIRECT_API_BASE=http://5.78.146.122:3006/v1
